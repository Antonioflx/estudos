function doStuff(values: ReadonlyArray<string>) {
	// const cop = values.slice();
	console.log("The first value is " + values[0]);

	// values.push("Hello!") // erro, pq readOnlyArray, n pode alter
}
