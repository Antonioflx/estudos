const nome = <String>"Antonio"; // intuitivo que so pode String
const age = <Number>22; // intuito que só pode numero
const boolean = <Boolean>true; // intuito que só pode boolean
const qualquer_um = <any>2; // esse pode qualquer um.

console.log(nome);
console.log(age);
console.log(boolean);
console.log(qualquer_um);
