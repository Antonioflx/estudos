const somar = (first_value: number, second_value: number): number =>
	first_value + second_value;

console.log(somar(2, 3));

function getName(name: string): string {
	return "Seu nome é " + name;
}

console.log(getName("Antonio"));

export {};
