 let x;

 // Array Literal
 const numbers = [12, 45, 33, 29 , 39];

// Array Constructor
const fruits = new Array('apple', 'orange', 'banana');

x = fruits[2]; // acessar a array, utiliza o indice dentro do []

x = `Minha fruta favorita é a ${fruits[2]}`;

// mostrar a quantidade de valores dentro de uma array
x = numbers.length;

// adicionar um valor na array


 console.log(x);