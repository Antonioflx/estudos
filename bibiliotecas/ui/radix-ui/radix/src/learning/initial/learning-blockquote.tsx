import { Blockquote } from "@radix-ui/themes";

export default function LearningBlockquote() {
	return (
		<Blockquote size={{ initial: "3", sm: "6" }} color="red">
			Isso é um bloco de texto responsivo
		</Blockquote>
	);
}
